// apps/ui/src/components/ui/index.ts

export * from './input';   // Input has default too, but named is enough
export * from './table';   // Table has default too, but named is enough

export * from './button';  // 🔧 keep named only; your file has no default
// (remove) export { default as Button } from './button';

export * from './Card';    // 🔧 keep named only; your file has no default
// (remove) export { default as Card } from './Card';
